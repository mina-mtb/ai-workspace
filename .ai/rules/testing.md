# Testing

- Every new behaviour gets a test. Every bug fix gets a regression test.
- Run the project's test command (see root `AGENTS.md`) before declaring done.
- Do not weaken or delete a test just to make the suite pass. If a test is wrong, fix it
  and explain why in your report.
- Prefer fast unit tests; add integration tests where components meet (DB, API, services).
